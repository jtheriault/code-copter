'use strict';
var codeCopter = require('../');

// Analyze the default set of code files with the default analyzers and use the
// default reporter for the results.
describe('Simple Example', codeCopter);
