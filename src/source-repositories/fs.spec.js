'use strict';
xdescribe('File system source repository', function describeFsSourceRepository () {
    it('should get all valid source', function getAll () {
        fail('untested');

        // TODO: expect folder path
        
        // TODO: expect result length to match mock length

        // TODO: expect result locations to match mock file paths
    });

    it('should read files when getting source lines', function getLines () {
        fail('untested');
    });

    it('should exclude configured folders', function getAll () {
        fail('untested');
    });

    it('should ignore non-js files', function getAll () {
        fail('untested');
    });
});
